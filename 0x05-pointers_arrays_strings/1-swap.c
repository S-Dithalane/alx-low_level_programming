#include "main.h"

/**
 * swap_int - Swap two integers
 * @a: Pointer to the parameter to be swapped
 * @b: Pointer to the parameter to be swapped
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int swap;

	swap = *a;
	*a = *b;
	*b = swap:
}
