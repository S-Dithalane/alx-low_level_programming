#include "main.h"

/**
 * _abs - Computes the absolute value of integer
 * @int: Parameter data type
 * Return: The absolute integer value
 */
int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
