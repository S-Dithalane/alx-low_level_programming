#include "main.h"

/**
 * add - adds two integers
 * @i: int to be added r
 * @r: int to be added to i
 * Return: results
 */
int add(int i, int r)
{
	int results;

	results = i + r;

	return (results);
}

